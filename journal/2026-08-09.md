# 2026-08-09

## Rejected-candidate explanation sidecars

- Replaced the universal appended-diagnostic contract with clean, byte-for-byte rejected candidates and same-basename `.explanation.md` sidecars.
- Sidecars identify the candidate path and SHA-256 and retain run, entity, artifact, stage, session, attempt, timestamp, failure class, authority, validator/reviewer, rejection code, actionable explanation, retry disposition, report, validation, and thread evidence.
- Rejected filenames now use artifact, run, and pipeline-attempt identity without a redundant numeric ordinal.
- Added guarded pair persistence that never overwrites either file, removes only a newly created candidate after sidecar failure, and refuses to disturb a pre-existing sidecar.
- Advanced machine run evidence to schema version 3. New evidence uses `sidecar`; immutable historical appended evidence is read as `legacy_appended` and is never rewritten.
- Updated governance, operational/investigation guidance, package evidence policy, templates, reference runtime, README, schemas, and adversarial fixtures.
- Verified 56 unit tests, 25 lint-clean prompts, Python compilation, and the schema-v2 governance-conformant reference package with no warnings.

## Sequential rejection evidence bundles

- Replaced opaque run/attempt filenames with entity-local numeric bundles such as `resume.1.rejected.md` and typed precursor names such as `resume.1.rejected.claim_review.json`; trace UUIDs and hashes remain in sidecars and reports.
- Added atomic numeric allocation, guarded parent/precursor bundle persistence, truthful JSON/text/Markdown/PDF extension classification, exact-byte preservation, and reusable non-progress detection.
- Advanced run evidence to schema version 4 with sequence, role, content format, parent/child linkage, checkpoint, and reconciliation fields; advanced pipeline packages to schema version 3 with a required precursor pattern.
- The reference runner checkpoints after rejection and entity events and reconciles stale `running` runs as interrupted after safely acquiring the runner lock.
- Governance now requires parent-candidate preservation after downstream failure and separately exact citations or stable IDs for multi-passage semantic evidence.
- Historical appended and run/attempt-named evidence remains immutable legacy evidence.
